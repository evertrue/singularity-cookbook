source 'https://supermarket.chef.io'

metadata

group :integration do
  # %w(
  #   et_mesos_slave
  #   et_mesos
  # ).each { |cb| cookbook cb, path: "../#{cb}" }

  cookbook 'zookeeper_minimal', path: 'test/integration/cookbooks/zookeeper_minimal'
end
